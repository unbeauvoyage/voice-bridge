# EditorConfig — style + analyzer severities for the .NET backend experiment
# (MessageRelay, VoiceBridge, ContentService, BackendShared).
#
# Pre-existing C# in AppHost/ and ServiceDefaults/ is unaffected at runtime
# because EnforceCodeStyleInBuild is only set in Backends.props (which AppHost
# and ServiceDefaults do not import). Severities here only fire when a project
# opts in.

root = true

# ─── Common ───────────────────────────────────────────────────────────────────

[*]
charset = utf-8
end_of_line = lf
insert_final_newline = true
trim_trailing_whitespace = true
indent_style = space

[*.{cs,csproj,props,targets,sln,xml,yml,yaml,json}]
indent_size = 2

[*.cs]
indent_size = 4

# ─── C# language conventions ──────────────────────────────────────────────────

[*.cs]

# File-scoped namespaces — modern, less indentation.
csharp_style_namespace_declarations = file_scoped:error

# Forbid loose `var` — every local must declare its type explicitly. This is the
# closest C# analogue to the TS-side ban on `any`: `var` lets the compiler hide
# the type from a reader; explicit types put the type back in the source.
csharp_style_var_when_type_is_apparent = false:error
csharp_style_var_for_built_in_types = false:error
csharp_style_var_elsewhere = false:error

# Prefer expression-bodied members where the body is a single expression.
csharp_style_expression_bodied_methods = when_on_single_line:suggestion
csharp_style_expression_bodied_constructors = when_on_single_line:suggestion
csharp_style_expression_bodied_operators = when_on_single_line:suggestion
csharp_style_expression_bodied_properties = true:suggestion

# Modern null-handling.
csharp_style_throw_expression = true:error
csharp_style_conditional_delegate_call = true:error

# `using` directives outside the namespace (modern, but keep flexibility).
csharp_using_directive_placement = outside_namespace:warning

# Pattern matching > old-style cast checks.
csharp_style_pattern_matching_over_is_with_cast_check = true:error
csharp_style_pattern_matching_over_as_with_null_check = true:error

# ─── .NET style conventions ───────────────────────────────────────────────────

dotnet_style_qualification_for_field = false:error
dotnet_style_qualification_for_property = false:error
dotnet_style_qualification_for_method = false:error
dotnet_style_qualification_for_event = false:error
dotnet_style_predefined_type_for_locals_parameters_members = true:error
dotnet_style_predefined_type_for_member_access = true:error

# Unused using directives are errors in projects that opt in.
dotnet_diagnostic.IDE0005.severity = error

# ─── Nullability — every nullable warning is an error ────────────────────────

dotnet_diagnostic.CS8600.severity = error  # converting null literal to non-nullable
dotnet_diagnostic.CS8601.severity = error  # possible null reference assignment
dotnet_diagnostic.CS8602.severity = error  # dereference of possibly null reference
dotnet_diagnostic.CS8603.severity = error  # possible null reference return
dotnet_diagnostic.CS8604.severity = error  # possible null reference argument
dotnet_diagnostic.CS8618.severity = error  # non-nullable field uninitialized
dotnet_diagnostic.CS8625.severity = error  # cannot convert null to non-nullable

# ─── Banned: `dynamic` (analogue to TS `any`) ─────────────────────────────────
# `dynamic` defers type-checking to runtime — the C# escape hatch we don't want
# in backend services. Diagnostic CA1859 / IDE0072 warn on dynamic in some shapes;
# the cleanest hard-ban is enforced via code review until a custom analyzer is
# wired (CS0117 et al only fire on actual misuse).

# CA1019 prefer typed; CA1858 use StartsWith over IndexOf; etc. — all errors via
# AnalysisMode=All in Backends.props.

# ─── Banned: null-forgiving `!` operator (analogue to TS `!`) ─────────────────
# C# 8+ `!` (null-forgiving) tells the compiler "trust me, not null". The cleanest
# enforcement is to keep nullable warnings as errors (above) — `!` is then only
# valid where a real invariant is documented; reviewers reject undocumented uses.
# A hard analyzer for this lives in the community Roslynator/StyleCop packages
# (e.g., RCS1228, SA0001) — those are enabled via AnalysisMode=All in Backends.props.

# ─── Banned: dangerous `as` casts (analogue to TS `as`) ───────────────────────
# Roslynator RCS1206: avoid `as` cast when the value is dereferenced without a
# null check. Promoted to error here; pattern-matching `is T t` is the
# preferred form (see csharp_style_pattern_matching_over_as_with_null_check above).
dotnet_diagnostic.RCS1206.severity = error

# ─── Documentation — relaxed for now ──────────────────────────────────────────
# CS1591 is silenced via NoWarn in Backends.props; we don't yet demand XML docs
# on every public symbol. Keep CS1574/CS1580 (broken cref) at error so existing
# docs don't rot silently.
dotnet_diagnostic.CS1574.severity = error
dotnet_diagnostic.CS1580.severity = error
dotnet_diagnostic.CS1581.severity = error
dotnet_diagnostic.CS1584.severity = error
